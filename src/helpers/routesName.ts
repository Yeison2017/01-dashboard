export enum MainRoutesName {
  main = "/",
  team = "/team",
  contacts = "/contacts",
  invoices = "/invoices",
  form = "/form",
  calendar = "/calendar",
  faq = "/faq",
  bar = "/bar",
  pie = "/pie",
  line = "/line",
  geography = "/geography",
}
